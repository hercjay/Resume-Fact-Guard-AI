const nextConfig = {
  // Hides the floating Next.js dev tools badge in the corner. Dev-only UI; it never
  // ships in a production build either way.
  devIndicators: false,
};

export default nextConfig;
